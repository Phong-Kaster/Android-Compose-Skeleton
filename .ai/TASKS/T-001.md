# T-001 — App posts a "hello world" notification on every cold start

- **Status:** verified except one residual — DoD c3 denial-path runtime check needs a human decision (E-003, see `.ai/ESCALATION.md`)
- **Depends on:** DoD approval (granted 2026-07-17) · capability ledgers materialized (done 2026-07-17, E-002)
- **Attempts:** 2 / 3

## Description

Add the missing posting side of notifications and wire it to cold start:

- `ui/util/NotificationUtil.kt` — reusable `object` (mirror of `PermissionUtil`): `createChannel(context)` guarded for API 26+ via `NotificationChannelCompat`, and `postSimpleMessage(context, title/message)` using `NotificationCompat.Builder` + `NotificationManagerCompat.notify`. Must check `isNotificationGranted(context)` (existing helper) and silently no-op when not granted — never crash with `SecurityException`.
- Strings appended to end of `strings.xml`: `hello_world`, channel name/description keys.
- Call site: `MainApplication.onCreate()` — runs exactly once per process, which is the cold-start semantics the PRD asks for (warm starts / config changes do not re-run it).
- KDoc with `@author Phong-Kaster` on every new class/function; README package tree updated with the new file.

## Acceptance

- DoD criteria 1–4 satisfied at code level (trigger location, once-per-process, permission guard, channel guard).
- DoD criterion 5: `.\gradlew.bat assembleDebug` → BUILD SUCCESSFUL (requires standing capability grant).
- DoD criterion 6: repo-rule checklist recorded here.
- If `adb` capability granted: runtime evidence per DoD table; otherwise mark runtime criteria "pending human manual check" in STATE.md.

## Evidence

Implementation (Iteration 2, 2026-07-17):

- `app/src/main/java/com/example/skeleton/ui/util/NotificationUtil.kt` — new. `createChannel` uses `NotificationChannelCompat` (no-op below API 26 → DoD c4); `postSimpleMessage` returns early via `isNotificationGranted(context)` and additionally wraps `notify()` in try/catch `SecurityException` (→ DoD c3, fail-soft). Reuses existing helper — no duplicated permission logic.
- `MainApplication.kt` — `showColdStartNotification()` (private, KDoc'd) called at the end of `onCreate()` → exactly once per process creation (→ DoD c1, c2).
- `strings.xml` — appended at end: `hello_world` = "hello world" (→ DoD c1 text), `skeleton` (title), `important_updates_and_alerts` (channel description). Channel name reuses existing `notification` string. Names describe content per repo rule.
- `README.md` — `ui/util` line now lists `NotificationUtil`.

Repo-rule checklist (DoD c6):

- [x] KDoc + `@author Phong-Kaster` on `NotificationUtil`, both functions, and `showColdStartNotification`.
- [x] Strings appended to end of `strings.xml`; names describe content, not feature.
- [x] UI-only helper placed in `ui/util/` (matches `PermissionUtil` placement).
- [x] No new dependencies (androidx core only, per DoD constraint).
- [x] Existing `HomeRequestPermission` flow untouched.
- [x] README package tree synced.
- [x] Kid-friendly comments on the non-obvious parts (channel no-op below 26, once-per-process reasoning).

Verification (Iteration 3, 2026-07-17, device `b56e2819` = physical Android 15 / API 35):

- [x] DoD c5: `./gradlew.bat assembleDebug` → **BUILD SUCCESSFUL in 50s** (39 tasks; only pre-existing Kotlin annotation-target warnings, none in new code).
- [x] DoD c1 runtime: APK installed (`adb install -r` → Success); `am force-stop` + launch of `com.example.myapplication/com.example.skeleton.MainActivity`; `dumpsys notification --noredact` shows NotificationRecord pkg=com.example.myapplication id=1 channel=`skeleton_general` with `android.title=String (Skeleton)` and `android.text=String (hello world)` — exact text match.
- [x] DoD c2 runtime: HOME → relaunch (warm start, same PID 32633 before/after); the full set of `mCreationTimeMs` values across all device notifications is identical before vs after (each value appears exactly once per snapshot), and our record's `mCreationTimeMs == mUpdateTimeMs` unchanged → nothing was posted or updated on warm start.
- [x] DoD c3 (granted half): permission granted (USER_SET via the app's own bottom-sheet flow); cold start posts the notification — same evidence as c1 ("next cold start after granting posts").
- [ ] DoD c3 (denied half, runtime): **blocked by device** — `pm grant`/`pm revoke` throw SecurityException (shell lacks GRANT/REVOKE_RUNTIME_PERMISSIONS; OEM restriction), and `cmd appops set POST_NOTIFICATION deny` is silently reverted. Code-review half of c3's evidence is complete (early-return on `isNotificationGranted`, which is `checkSelfPermission(POST_NOTIFICATIONS)` on API 33+, plus try/catch `SecurityException` around `notify()`). Escalated as E-003: human 10-second manual check, OEM debug toggle, or accept code review.
- [x] DoD c4: record posted through channel `skeleton_general` on API 35 (runtime); `NotificationChannelCompat` is a documented no-op below API 26 (code review); build passes with minSdk 24 (c5 output). Evidence spec for c4 (code review + build) fully satisfied.

## Notes

- Discovery at bootstrap: permission request flow already exists (`HomeRequestPermission.kt`); do not duplicate it. Only the posting side is missing.
- `POST_NOTIFICATIONS` already declared in `AndroidManifest.xml` — no manifest permission change needed.
- Discovery at iteration 2: `applicationId` is `com.example.myapplication` while the source `namespace` is `com.example.skeleton` (`app/build.gradle.kts`). The escalation's adb scope (`com.example.myapplication`) is correct for device commands; `knowledge/PROJECT.md` corrected to record both.
